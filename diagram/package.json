{
	"name": "yang-eclipse",
	"version": "0.0.1",
	"description": "sprotty diagrams for the Yang DSL in Eclipse",
	"author": "typefox.io",
	"license": "Apache-2.0",
	"private": true,
	"keywords": [
	  "sprotty", "yang", "diagram"
	],
	  "devDependencies": {
		"lerna": "^2.1.1"
	},
	"scripts": {
		"prepare": "lerna run prepare",
		"update": "git pull origin/master && git submodule foreach git pull origin/master"
	},
	"workspaces": [
		"sprotty/client",
		"yang-sprotty",
		"yang-standalone"
	]
}
